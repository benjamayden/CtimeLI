"""Pure domain logic — no I/O, no platform calls, no ambient clock.

Everything here is deterministic given its arguments. This is the layer that
ports verbatim to another language. See docs/domain.md.
"""
